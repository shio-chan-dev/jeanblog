interface:
  display_name: "Algorithm Problem ACERS Blogger"
  short_description: "v0.1.5 - Write guided-build ACERS problem posts"
  default_prompt: "Use $algorithm-problem-acers-blogger to turn this concrete algorithm problem into a publishable Hugo ACERS post that starts with the problem block, then teaches with a step-by-step guided build, assembled full code, reference answer, no standalone naive-idea section, the correct source-specific path, and complete front matter."
